/*
* loading 动画指令, 使用css3, ie9及以下浏览器不支持动画效果
* */
define(['angularAMD', 'css!loading.less'], function(angularAMD){

    angularAMD.directive("loading", function() {
        return {
            restrict: 'EA',
            template: __inline('loading.tpl'),
            replace:true
        };
    });
});